# Dependencies
node_modules/

# Environment and secrets
.env
.env.local
.env.*.local

# User-uploaded media (gallery) — keep directory via .gitkeep
uploads/gallery/*
!uploads/gallery/.gitkeep

# Logs and runtime
*.log
npm-debug.log*
.DS_Store

# Build / cache (if added later)
dist/
.cache/
